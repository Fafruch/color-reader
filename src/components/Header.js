import React from 'react';

const Header = () => (
  <div>
    <h1>Color reader</h1>
    <hr className="Header-line" />
    <h4>See & convert your <br /> HEX / RGB / HSL color</h4>
  </div>
);

export default Header;
